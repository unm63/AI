from llama_index.core import VectorStoreIndex, SimpleDirectoryReader, Settings
from llama_index.core.llms import MockLLM
from llama_index.embeddings.huggingface import HuggingFaceEmbedding

# 1. Set explicit local models that don't need keys
Settings.embed_model = HuggingFaceEmbedding(model_name="BAAI/bge-small-en-v1.5")
Settings.llm = MockLLM(max_tokens=20) # A lightweight local placeholder to bypass OpenAI checks

# 2. Run the pipeline
print("Ingesting local data documents...")
documents = SimpleDirectoryReader("data").load_data()

print("Building vector indexing...")
index = VectorStoreIndex.from_documents(documents)

query_engine = index.as_query_engine()

print("\nPipeline built successfully! Everything is loaded locally.")
